import { Button } from "./components/ui/button";

function App() {
  return (
    <div>
      <Button
        variant="primary"
        size="md"
        text="share"
        onClick={() => {}}
      />
      <Button 
        variant="secondary"
        size="sm"
        text="Add Content"
        onClick={() => {}}/>
      
    </div>
  );
}

export default App;
